import * as React from 'react';
import { DefaultButton } from 'office-ui-fabric-react';
import { useBoolean } from '@uifabric/react-hooks';
var volume0Icon = { iconName: 'Volume0' };
var volume3Icon = { iconName: 'Volume3' };
export var ButtonToggleExample = function (props) {
    var disabled = props.disabled, checked = props.checked;
    var _a = useBoolean(false), muted = _a[0], setMuted = _a[1].toggle;
    return (React.createElement(DefaultButton, { toggle: true, checked: muted || checked, text: muted ? 'Volume muted' : 'Volume unmuted', iconProps: muted ? volume0Icon : volume3Icon, onClick: setMuted, allowDisabledFocus: true, disabled: disabled }));
};
//# sourceMappingURL=Button.Toggle.Example.js.map